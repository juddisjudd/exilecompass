.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Paraglide
src/lib/paraglide
project.inlang/cache/
# Generated from guides/*.yaml by tools/build-crafting.mjs (run via `bun run crafting`)
.claude
.vscode
CLAUDE.md
# Local release notes (Defender false-positive submission template + log)
MS-DEFENDER-SUBMISSION.md
# Tauri updater signing keys — NEVER commit the private key
src-tauri/updater-key.txt
src-tauri/updater-key.txt.pub
./src-tauri/updater-key.txt
./src-tauri/updater-key.txt.pub
scripts/*.mjs
# release.mjs is run by regex-sync.yml in CI, so it has to be tracked.
!scripts/release.mjs
resources
# The bundled keyword-spotting model (voice commands) needs to actually be
# committed — see src-tauri/resources/kws/README.md for what it is. Narrow
# exception rather than removing the broad `resources` rule above, since it's
# unclear what else that was meant to exclude.
!src-tauri/resources/
!src-tauri/resources/**
AGENTS.md
docs
.remember
# offline voice-recognition eval renders (tools/kws-eval/synth.ps1)
tools/kws-eval/out/
